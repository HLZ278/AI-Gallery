# v1.8.4

**发布日期**：2026-05-29

## 品牌与安装目录

- npm 包名 / 安装目录由 `your-picture` 更名为 **`ai-picture`**
- 应用 ID：`com.aipicture.app`
- 用户数据目录：`%APPDATA%/AiPicture`（自动从 `YourPicture` 迁移）
- 数据库文件：`ai-picture.db`（自动从 `yourpicture.db` 重命名）

## 修复

- **新机器安装崩溃**：修正打包后 `ollama-runtime.json` 等配置文件路径解析（统一 `app.getAppPath()`）
- 同步修正 `inference-devices.json`、`analysis-limits.json` 的 bundled config 路径

## 升级说明

- 覆盖安装后安装目录为 `ai-picture`，旧版 `your-picture` 目录可手动删除
- 配置与数据库会自动迁移，无需重新配置
