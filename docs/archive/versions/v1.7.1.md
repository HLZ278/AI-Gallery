# v1.7.1

**日期**: 2026-05-26

## 亮点

- **提示词自动最新**：扫描 `prompts/` 目录自动选用最高版本，设置页不再手动选版本
- **搜索网格修复**：虚拟滚动网格重叠、加载后空白等问题
- **图库监控修复**：补全 `libraryWatcherService` 主进程导入，启动不再报错
- **抽帧默认值**：视频 8 帧、GIF 5 帧、帧序列 fps 2

## 相关变更

- 新增 `PromptRegistry`：按文件名解析并比较提示词版本
- `MediaGrid`：固定单元格尺寸、`fillCell` 模式、容器宽度测量修复
- 配置移除 `promptVersion` / `videoPromptVersion` / `gifPromptVersion` / `llmSearchPromptVersion`
